	#Запись строки в файл

	def Write():
		text = input("Введите текст для записи: ")
		print("Запись текста:\t", text)
		text_file = open("file.txt", "w", encoding='utf-8') #Режим "w" чистит всю информацию файла, если он уже существует
		text_file.write(text)
		text_file.close()

	def Read():
		text_file = open("file.txt", "r", encoding='utf-8') #Если такого файла не существует, будет выведена ошибка
		text = text_file.read()
		print("Прочитанный текст:\t", text)
		text_file.close()

	print("Выберите режим:\n[1] Запись текста в файл\n[2] Чтение текста из файла")
	a = int(input())
	if a == 1:
		Write()
	elif a == 2:
		Read()
	else:
		print("Неправильный ввод")

	#Режим "a" - как "w", но он не удаляет информацию, а дописывает новые файлы 
	#Режимы "r+, w+" - то же самое, что "r" и "w" вплоть до ошибок и замены данных, но также даётся возможность записать или прочесть файл
	#Режим "a+" - это "a", но можно также прочитать файл